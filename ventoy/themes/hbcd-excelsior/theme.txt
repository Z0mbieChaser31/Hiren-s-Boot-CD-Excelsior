# ============================================================
# Hiren's Boot CD Excelsior — GRUB2 Theme
# A modern, dark rescue toolkit boot theme
# Compatible with Ventoy's GRUB2 menu system
# ============================================================

# Desktop image / background
desktop-image: "background.png"
desktop-color: "#0d1117"

# Default typeface
# (fonts must be listed in ventoy.json)
title-font: "DejaVu Sans Bold 22"
message-font: "DejaVu Sans 14"

# Global colors
terminal-box: "terminal_box_*.png"

# ── Title (at top of screen) ────────────────────────────────
+label {
    left = 0
    top = 3%
    width = 100%
    height = 40
    align = center
    id = __title__
    text = "⚡ Hiren's Boot CD Excelsior"
    font = "DejaVu Sans Bold 28"
    color = "#1a9de0"
}

+label {
    left = 0
    top = 8%
    width = 100%
    height = 28
    align = center
    text = "Select a rescue environment to boot"
    font = "DejaVu Sans 14"
    color = "#8b949e"
}

# ── Boot menu ──────────────────────────────────────────────
+boot_menu {
    left = 5%
    top = 15%
    width = 90%
    height = 62%

    item_font = "DejaVu Sans 16"
    item_color = "#c9d1d9"
    selected_item_color = "#ffffff"
    selected_item_pixmap_style = "select_*.png"

    icon_width = 32
    icon_height = 32
    icon_space = 12

    item_height = 42
    item_padding = 16
    item_spacing = 4

    scrollbar = true
    scrollbar_width = 8
    scrollbar_thumb = "scrollbar_thumb.png"
}

# ── Countdown timer ─────────────────────────────────────────
+label {
    left = 0
    top = 80%
    width = 100%
    height = 24
    align = center
    id = __timeout__
    text = "Boot in %d seconds — press any key to cancel"
    font = "DejaVu Sans 13"
    color = "#58a6ff"
}

# ── Help text ───────────────────────────────────────────────
+label {
    left = 0
    top = 85%
    width = 100%
    height = 22
    align = center
    text = "↑↓ Navigate   Enter Select   F2 Language   F5 Directory Tree   F6 Details"
    font = "DejaVu Sans 12"
    color = "#6e7681"
}

# ── Version watermark ───────────────────────────────────────
+label {
    left = 0
    top = 92%
    width = 100%
    height = 20
    align = center
    text = "Hiren's Boot CD Excelsior v2.0 — github.com/HBCD/Hiren-s-Boot-CD-reborn"
    font = "DejaVu Sans 11"
    color = "#30363d"
}
